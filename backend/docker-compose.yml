spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:
      timeout: 60000
      jedis:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

application:
  security:
    jwt:
      secret-key: your-secret-key-here
      expiration-ms: 3600000
      refresh-token:
        expiration-ms: 604800000